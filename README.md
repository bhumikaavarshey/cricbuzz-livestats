# Cricbuzz LiveStats

Multi-page Streamlit cricket analytics application with API integration, SQLite database, 25 SQL analytics queries, visualizations and CRUD.

Run: `pip install -r requirements.txt` then `streamlit run app.py`

The app runs in demo mode without an API key. Add the RapidAPI key in api.py when available for live API requests.